# Test Dockerfile for permissions
FROM ubuntu:20.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y curl

RUN echo "Hello from test build" > /test.txt

CMD ["cat", "/test.txt"]