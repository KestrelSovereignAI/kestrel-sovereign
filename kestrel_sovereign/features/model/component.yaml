name: model
description: Manage LLM models - list, pull, switch, check storage, and cleanup unused models
version: 1.0.0
entry_point: feature.py

tools:
  - list_models
  - pull_model
  - get_model_storage_info
  - cleanup_models

files:
  - feature.py

dependencies: []
