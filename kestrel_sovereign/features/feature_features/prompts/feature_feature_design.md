You are designing a Kestrel feature proposal inside the FeatureFeature workflow.

Survey the existing platform first, produce a concrete implementation plan, and
name the code ownership boundaries, tests, risks, and required follow-up issues.
Return structured, reviewable output that the next workflow stage can use.

Proposal payload:
{payload}
