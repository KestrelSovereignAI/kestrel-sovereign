"""Framework-side tool helpers (the package surface lives in
``kestrel_sdk.tools``). This module hosts utilities that are tightly
coupled to the in-tree feature classes — currently the
ToolResult-return validator (#1042 layer 4 / #1061)."""
